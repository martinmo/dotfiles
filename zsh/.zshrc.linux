## systemctl is a nightmare to type *and* complete
if [[ -x /usr/bin/systemctl ]]; then
    # $SUDO will be set by grml zshrc
    alias sc="$SUDO systemctl"
    alias jc="$SUDO journalctl"
fi

## Basic `ls` settings
alias ls='ls --color=auto -b -CF'
